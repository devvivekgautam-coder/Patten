#include <stdio.h>

int main () {

    int i, j, a;
    char ch ='a';

    for (i = 5; i >= 1;i--) {
        for (int s = i; s >= 1 ; s--) {
            printf("_ ");
        }
        for (j = i; j >= a; j--) {
            if (i <= 'z') {
                printf("%c", a);
            }
            printf("%d ", j);
        }
        printf("\n");
    }
    return 0;
}
